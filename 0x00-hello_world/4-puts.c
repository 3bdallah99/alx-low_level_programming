#include <stdio.h>
/**
 * main - this is main func in the app
 *
 * description: A c program that print with puts
 *
 * Return: always 0 (success)
 *
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
