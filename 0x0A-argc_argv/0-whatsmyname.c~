include < stdio.h >
/**
 *
 * main - prints its name, followed by a new line.
 * description -  it will print the new name,
 * without having to compile it again
 * return: string name
 */
int main(int argc, string argv[])
{
printf("%s\n", argv[0]);
return (0);
}
